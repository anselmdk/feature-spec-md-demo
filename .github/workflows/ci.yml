name: CI

on: [push, pull_request]

permissions:
  contents: read

jobs:
  verify:
    uses: anselmdk/feature-spec-md/.github/workflows/feature-spec-ci.yml@main
    with:
      tests: "tests/**/*.ts"
    secrets: inherit
